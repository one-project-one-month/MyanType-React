/*
export default function TypingSpeedPanel({ speeds, stats }) {
    return (
        <div className={'flex gap-12'}>
            {speeds.map((item, i) => (
                <div key={i} className={'flex flex-col'}>
                    <p className={'text-sm font-normal'}>{item.time} seconds</p>
                    <h1 className={'text-3xl'}>{item.wpm} WPM</h1>
                    <p className={'text-xl opacity-70'}>{item.accuracy}%</p>
                </div>
            ))}
        </div>
    );
}*/
